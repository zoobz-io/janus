# Air hot-reload config for the admin API (cmd/admin).
root = "."
tmp_dir = "tmp-admin"

[build]
  bin = "./tmp-admin/main"
  cmd = "go build -o ./tmp-admin/main ./cmd/admin"
  delay = 1000
  exclude_dir = ["assets", "tmp", "tmp-api", "tmp-admin", "vendor", "docker", "migrations"]
  exclude_regex = ["_test.go"]
  include_ext = ["go", "tpl", "tmpl", "html"]
  kill_delay = "2s"
  log = "build-errors-admin.log"
  stop_on_error = true

[color]
  build = "yellow"
  main = "magenta"
  runner = "green"
  watcher = "cyan"

[log]
  time = false

[misc]
  clean_on_exit = true
